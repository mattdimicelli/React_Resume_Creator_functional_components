import React from 'react';

function TitleBar(props) {
        return (
            <div className='title-bar'>
                {props.title}
            </div>
        );
}

export default TitleBar;
    